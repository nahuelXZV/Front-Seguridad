import { Component } from '@angular/core';

@Component({
  selector: 'shared-nav',
  templateUrl: './nav.component.html',
})
export class NavComponent {

  constructor(
  ) { }

  logout(): void {
  }
}
